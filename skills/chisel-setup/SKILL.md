---
name: chisel-setup
description: Placeholder for the Chisel setup skill — first-time bootstrap of a Chisel theme's design tokens and structure. Real content supplied later.
---

# chisel-setup (placeholder)

> **Placeholder skill.** This file exists so the package bundles a valid, auto-discovered skill. Replace its body (and refine the `description` above) with the real setup-skill content; keep the `name` equal to this directory name (`chisel-setup`). If the real skill is named differently (e.g. `chisel-setup-theme-json`), rename this directory and update `name` to match.

This skill will bootstrap a Chisel theme's initial design tokens and structure.
