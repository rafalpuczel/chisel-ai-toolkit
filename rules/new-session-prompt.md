# New Session Prompt — Chisel AI Toolkit

> **Placeholder.** This primer is copied to `.claude/new-session-prompt.md` in a consumer project. Paste it (or its key points) at the start of a new AI session to orient the agent on this project's Chisel setup. Replace with the real primer later.

When starting a new session on a Chisel theme:

1. Read the root `CLAUDE.md` for the active team rules.
2. The Chisel skills live under `.claude/skills/` — use `chisel-plan` to create progress files before multi-phase work.
3. Track ongoing work in `ai-progress/` so context survives across sessions and `/compact`.
